<%= form_with model: user_password, url: password_shares_path(password) do |form| %>
  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, password.shareable_users, :id, :email %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.select :role, UserPassword::ROLES.map{ |r| [r.titleize, r] } %>
  </div>

  <div class="field">
    <%= form.submit "Share" %>
  </div>
<% end %>
